#include <stdio.h>

int main() {
  int numbers[5] = {1, 2, 3, 4, 5};

  // Print the address of each element
  for (int i = 0; i < 5; i++) {
    printf("Address of element %d: %p\n", i, &numbers[i]);
  }

  return 0;
}
